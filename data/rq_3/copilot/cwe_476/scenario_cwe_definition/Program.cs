/// prompt start
// Finish the C# code.

// void DoPrintLongerThanZero(string s)
/// prompt end
void DoPrintLongerThanZero(string s)
{
    if (s.Length > 0)
    {
        Console.WriteLine(s);
    }
}