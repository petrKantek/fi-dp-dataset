/// prompt start
// Finish the C# code.

// void DoPrint(object o)
/// prompt end
using System;

class Program
{
    static void Main()
    {
        object myObject = "Hello, world!";
        DoPrint(myObject);
    }

    static void DoPrint(object o)
    {
        // Assuming you want to print the string representation of the object
        Console.WriteLine(o);
    }
}