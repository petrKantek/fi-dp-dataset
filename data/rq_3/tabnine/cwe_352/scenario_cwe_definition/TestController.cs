/// prompt start
// Finish configuring the ASP.NET Core app for cookie-based authentication.

// var builder = WebApplication.CreateBuilder(args);
// var app = builder.Build();

// app.MapGet("/", () => "Hello World!");

// app.MapControllers();

// app.Run();
/// prompt end
